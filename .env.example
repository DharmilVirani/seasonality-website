# Database configuration
DATABASE_URL="postgresql://admin:admin123@100.114.145.101:5432/youngturtle"

# Backend configuration
BACKEND_PORT=3001
NODE_ENV=development

# Frontend configuration
NEXT_PUBLIC_API_URL=http://localhost:3001

# =====================================================
# MINIO Configuration (S3-compatible object storage)
# =====================================================
MINIO_ENDPOINT=100.114.145.101
MINIO_PORT=9000
MINIO_USE_SSL=false
MINIO_ACCESS_KEY=admin
MINIO_SECRET_KEY=admin12345
MINIO_BUCKET_UPLOADS=seasonality-uploads
MINIO_BUCKET_PROCESSED=seasonality-processed
MINIO_BUCKET_BACKUPS=seasonality-backups

# =====================================================
# Redis Configuration (for BullMQ queue)
# =====================================================
REDIS_HOST=100.114.145.101
REDIS_PORT=6379

# =====================================================
# Worker Configuration
# =====================================================
# Number of concurrent files to process
WORKER_CONCURRENCY=5
# Batch size for database inserts
DB_BATCH_SIZE=1000
# Temporary directory for file processing
TEMP_DIR=/tmp/seasonality-uploads
# Maximum file size for uploads (10MB default)
MAX_FILE_SIZE=10485760
